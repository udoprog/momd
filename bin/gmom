#!/usr/bin/python

if __name__ == "__main__":
    import os
    import sys

    if os.path.isdir(os.path.join(os.getcwd(), "python", "mom")):
        sys.path.insert(0, os.path.join(os.getcwd(), "python"))

    from mom.gui.application import Application

    app = Application()
    app.run()
